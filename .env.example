# Copy to .env (local) or set in your Vercel project settings.
# Leaving OPENAI_API_KEY unset keeps the app in local-only privacy mode.
OPENAI_API_KEY=
# Local Express AI-review server port (used by `npm run dev:all` only).
PORT=3001
